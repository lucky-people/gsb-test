【CI 失败】lzpack：解压几乎全部报 CRC32 校验失败

仓库里的 lzpack（纯 Python 标准库实现的 LZ77 压缩库）测试挂了 11 条，报错都长这样：

    lzpack.errors.FormatError: 偏移 342: CRC32 校验失败：期望 3B05C147，实际 60F70435
    ----------------------------------------------------------------------
    Ran 30 tests in 9.822s
    FAILED (errors=11)

复现：

    python3 -m unittest test_lzpack -v

规律：空输入、单字节这类完全没有匹配的数据能过；只要数据里出现长度 ≥ 3 的重复片段（也就是真的用到了 LZ77 匹配），就崩——有的直接 CRC 校验失败，有的解出来的内容和原文不一致。

你要做的：

1. 定位并修掉根因，让 `python3 -m unittest test_lzpack` 30 条全绿。
2. 不许改测试来迁就实现；也不许关掉或放宽 CRC、长度、偏移这些完整性校验来「让它过」。
3. 这些不变量必须保持：一次性 `compress`/`decompress` 与流式 `Compressor`/`Decompressor` 的结果逐字节相同；`decompress(compress(x)) == x`；空输入、单字节、全 0、随机数据、以及偏移正好等于窗口边界的重复数据都要往返一致；损坏或截断的流仍然抛 `FormatError`。
4. 确认这轮修复没有牺牲压缩率：README 里那三组数据（1 MB 随机 / 1 MB 重复 / 10 MB 日志）的压缩率不应变差。
5. 补一条针对本次根因的回归测试；如果 README 对相关格式约定写得不够清楚，一并补齐。

提示：`lzpack/codec.py` 顶部注释里对每个字段的含义有精确描述（包括匹配标签后面那个字段到底存什么），先把它和实现逐行对照一遍再动手。
